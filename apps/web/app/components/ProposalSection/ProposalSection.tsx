import styles from "./ProposalSection.module.css";

export function ProposalSection() {
  return (
    <div>
      ProposalSection
      <span className={styles.decorUnion} />
      <span className={styles.decorReact} />
    </div>
  );
}
